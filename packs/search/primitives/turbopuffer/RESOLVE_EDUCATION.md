# resolve_education

Resolve school names in a decomposed search payload to canonical education IDs.

Use before `apply_prefilters` when a query mentions schools by name, for example
`Stanford engineers`.
