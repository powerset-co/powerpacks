# resolve_education

Resolve school names in a decomposed search payload to canonical education IDs.

Use before `the typed Powerset runner` when a query mentions schools by name, for example
`Stanford engineers`.
