{
  "name": "company_signals",
  "type": "turbopuffer_namespace",
  "version": "2026-07-31",
  "namespace": "aleph_company_signals_v1",
  "document_grain": "company_signal",
  "description": "Company-level semantic signals used to identify matching companies before querying eligible people at those companies.",
  "vector": {"dimension": 1536, "distance_metric": "cosine_distance"},
  "attributes": [
    {"name": "id", "type": "string", "notes": "Canonical company id used by people.company_id filters."},
    {"name": "signals_semantic_text", "type": "string"},
    {"name": "allowed_operator_ids", "type": "string[]"},
    {"name": "vector", "type": "vector"}
  ],
  "filters": [
    {"field": "id", "operators": ["In"], "value_type": "string[]"},
    {"field": "allowed_operator_ids", "operators": ["ContainsAny"], "value_type": "string[]"}
  ],
  "rules": [
    "Search company signals first, then query the people namespace with matched company ids and every person-level hard filter.",
    "Never treat a company-signal document id as a person or position id."
  ]
}
