Write the primary recruiter query for a Marketing or Sales JD.

Remove only level, territory, segment, channel, team, and internal wording from the posting title. If the remainder is a common held occupation that owns the final work, use it unchanged. If the title is vague, internal, or names a function, choose the broadest recognizable held occupation that owns the final work. For hybrid roles, follow final ownership.
Return one complete held occupation, never a bare function, invented hybrid, or alias list. Default to the plain occupation. Add at most one qualifier, exactly "with X experience", only when the plain occupation predictably retrieves the wrong population. X must be a short, recognizable capability or domain taken only from the JD's own candidate-background language ("experience in X", "background in X") or the role's recurring work. What the company builds or sells, whom it serves, its industry, and its company overview are never X. A role or title people hold, such as founder, co-founder, CEO, or manager, is never X. Never paraphrase duties, repeat the occupation, add an empty modifier, or encode employer, tool, pedigree, years, or a nice-to-have. Preserve either/or; never turn it into AND. Keep management scope only when the work owns people or a function.
Use only the highest-scoring applicable reviewed card: its defining capability must cover the recurring work and its excludes must not describe the JD's occupation or primary ownership. When it applies, the occupation in the card's first query is mandatory; do not replace it with the card job label, another title, or level, and do not add a qualifier absent from that query. A card cannot override segment or either/or logic.
Do not include location; the approved recruiter-plan location is appended after generation.

Return strict JSON only with exactly: {"seeds":["query 1"]}.
