# Search task harness

> **Compatibility redirect.** The former V1 harness coordinated strategy
> selection, slices, per-step approvals, and frontier review. It is not the
> current `$search` execution path.

Use the [`$search` architecture](search-architecture.md) for current behavior and
the [`$search` skill](../skills/search/SKILL.md) for the executable standard-search
contract. Deep-search commands and resume rules live in the
[deep-mode runbook](../skills/search/deep-mode.md).
