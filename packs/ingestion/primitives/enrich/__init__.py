"""Enrich-stage primitives: shared LinkedIn profile enrichment.

`enrich_people` is the orchestrator + CLI; `models`, `rapidapi_client`,
`profile_cache`, and `profile_transforms` hold the config/manifest types, the
RapidAPI client, the profile cache, and the pure row transforms respectively.
"""
